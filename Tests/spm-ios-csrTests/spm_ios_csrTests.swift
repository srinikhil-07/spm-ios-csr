import XCTest
@testable import spm_ios_csr

final class spm_ios_csrTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(spm_ios_csr().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
